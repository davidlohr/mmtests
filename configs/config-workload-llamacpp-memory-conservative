export MMTESTS="llamacpp"

# List of monitors
export RUN_MONITOR=yes
export MONITORS_ALWAYS=
export MONITORS_GZIP="proc-vmstat mpstat"
export MONITORS_WITH_LATENCY="vmstat"
export MONITOR_UPDATE_FREQUENCY=10

# LLama.cpp benchmark - Conservative memory usage
export LLAMACPP_COMPUTE_BACKEND=cpu
export LLAMACPP_MIN_THREADS=1
export LLAMACPP_MAX_THREADS=$NUMCPUS
export LLAMACPP_BATCH_SIZE=512
export LLAMACPP_ITERATIONS=3
export LLAMACPP_PROMPT_TOKENS=512
export LLAMACPP_GEN_TOKENS=128

# Conservative memory usage - only use 25% of system memory
export LLAMACPP_MODEL_SIZE=$MEMTOTAL_BYTES*1/4